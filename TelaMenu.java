import java.awt.*;
import javax.swing.*;

public class TelaMenu extends JPanel {
    public TelaMenu() {
        setLayout(new BorderLayout());
        add(new JLabel("Menu Principal", SwingConstants.CENTER), BorderLayout.CENTER);
    }
}
